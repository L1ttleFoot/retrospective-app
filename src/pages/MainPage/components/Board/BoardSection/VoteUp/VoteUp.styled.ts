import styled from 'styled-components';
import {IconButton} from '../../../../../../components/IconButton';

export const VoteUp = styled.div`
    position: absolute;
    right: 5px;
    bottom: 0px;
    display: flex;
    flex-direction: row;
    align-items: center;
    font-size: 14px;
`;

export const VoteUpButton = styled(IconButton)`
    width: 28px;
    height: 28px;
`;
