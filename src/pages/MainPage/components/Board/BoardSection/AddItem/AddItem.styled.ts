import styled from 'styled-components';

export const Input = styled.textarea`
    border: none;
    font-size: 16px;
    height: 100%;
    resize: none;

    &:focus {
        outline: none;
    }
`;
